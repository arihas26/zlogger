name: zlogger
description: A lightweight, zone-based structured logger for Dart. Features context propagation, named loggers, and JSON output.
version: 1.0.0
repository: https://github.com/arihas26/zlogger

environment:
  sdk: ^3.0.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
